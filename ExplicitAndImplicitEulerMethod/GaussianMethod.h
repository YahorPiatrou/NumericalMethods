#pragma once

#include <iostream>
#include <vector>

using namespace std;

vector<double> SolveGaussMethod(vector<vector<double>> , vector<double> );
vector<double> abs(vector<double>);
vector<double> sqrt(vector<double>);
double max(vector<double>);
double min(vector<double> );
 